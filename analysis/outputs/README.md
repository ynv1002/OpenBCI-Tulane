# Analysis Outputs

This folder is now treated as scratch space for regenerated benchmark outputs.

The professor-facing summaries, models, and live example runs live under:

- `final_package/report/`
- `final_package/results/`
- `final_package/models/`
- `final_package/live_run_examples/`

If you rerun analysis scripts on this branch, they may recreate files here.
