# Models

This folder holds the runtime model artifacts used by the hybrid demo.

Current files:

- `realtime_clench_model.pkl`
- `realtime_clench_model.json`
- `clean_left_right_window_model.pkl`

Notes:

- the jaw artifact is the stronger reusable branch artifact
- the hand artifact is kept honestly as the current runtime/benchmark artifact even though it is still weak
